import { describe, it, expect } from 'vitest';
import { EventBackboneModule } from './index';

describe('EventBackboneModule', () => {
  it('is defined', () => {
    expect(EventBackboneModule).toBeDefined();
  });
});
